export const CONFIG = {
    // World
    WORLD_WIDTH: window.innerWidth,
    WORLD_HEIGHT: window.innerHeight,

    // Agent Defaults
    AGENT_SIZE: 8,
    DEFAULT_SPEED: 120, // Pixels per second
    MAX_FORCE: 250,     // Steering force strength
    VISION_RADIUS: 100,
    STARTING_ENERGY: 100,

    // Food
    FOOD_SIZE: 4,
    FOOD_VALUE: 30, // Energy gain
};
